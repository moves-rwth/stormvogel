from stormvogel import parametric, model
from stormvogel.stormpy_utils import mapping

try:
    import stormpy
except ImportError:
    stormpy = None

def gradient_descent(model: model.Model):
    stormpy_model = mapping.stormvogel_to_stormpy(model)

def parameter_lifting(model: model.Model):
    return True

def solution_function():
    #user can do this by simply taking the model checking result and then for initial state?
    return True
